# التحقق من حفظ وتعديل الفروع

## سبب الخلل السابق
كانت واجهة إنشاء الفرع ترسل حقلًا باسم `managerId` غير موجود في جدول `branches` المنشور، فكان الخادم يفشل برسالة SQL عند الحفظ. تم توحيد مخطط التطبيق مع الجدول الفعلي وإزالة الاعتماد على هذا الحقل.

## السبب النهائي والإصلاح الأخير

أظهر فحص قاعدة البيانات أن جدول `regions` كان فارغًا، بينما يفرض جدول `branches` مفتاحًا أجنبيًا على `regionId`. لذلك كان إدخال `regionId=1` يفشل حتى مع صحة بقية حقول الفرع. أصبح `branches.create` الآن يحل المنطقة بالمعرّف عند وجودها، ثم يبحث بالاسم، وينشئ سجل منطقة جديدًا عند عدم وجود تطابق قبل إنشاء الفرع. يغطي الاختبار هذه الحالة إلى جانب حالة المنطقة الموجودة.

## مسار الاستخدام

يدخل المستخدم إلى **الفروع** من القائمة الجانبية، ثم يضغط **إضافة فرع**. يظهر نموذج **إضافة بيانات فرع جديد** ويطلب رمز الفرع واسم الفرع والمدينة ومعرف المنطقة الرقمي واسم المنطقة، مع جعل اسم مدير الفرع اختياريًا. بعد استكمال الحقول يضغط المستخدم **حفظ الفرع**.

عند نجاح العملية تظهر رسالة **تمت إضافة الفرع بنجاح**، تُمسح الحقول، ويُعاد إبطال استعلام `branches.list` حتى ينعكس الفرع الجديد في دليل الفروع دون إعادة تحميل الصفحة. بعد ظهور الفرع يمكن اختيار **تعديل** من صفه، تعديل البيانات، ثم الضغط على **حفظ التعديل** أو **إلغاء**.

## الإصلاحات الأمنية والمرئية

أضيف الإجراء `branches.update` مع التحقق من وجود السجل والصلاحيات والتدقيق. لا يستطيع المستخدم العادي التعديل، ولا يستطيع مدير المنطقة تغيير `regionId` أو `region` أو نقل الفرع خارج نطاقه. تظهر حالات **جارٍ الحفظ** و**تم الحفظ** ورسالة خطأ عربية على مستوى صف الفرع، كما تظهر رسالة عربية مفهومة داخل نموذج إضافة الفرع عند فشل الإنشاء.

## أدلة التحقق

| المجال | النتيجة | الدليل |
|---|---|---|
| TypeScript | ناجح | `pnpm check` يمر دون أخطاء. |
| Vitest | ناجح | 43 اختبارًا ناجحًا ضمن 12 ملفًا. |
| إنشاء وتعديل الفرع | ناجح | `server/branches.create.test.ts` يثبت الإنشاء، التعديل، منع الدور غير المصرح، ومنع مدير المنطقة من تغيير نطاق الفرع. |
| تحديث دليل الفروع | ناجح | الواجهة تستدعي `utils.branches.list.invalidate()` بعد نجاح الإنشاء أو التعديل. مكوّن `BranchUpdateStatus` يعرض حالات pending/success/error للصف المطابق فقط، مع اختبار React render فعلي. |
| مخطط قاعدة البيانات المنشور | ناجح | تم فحص جدول `branches` فعليًا؛ يحتوي 15 عمودًا، ولا يوجد `managerId`، ويوجد `regionId` المستخدم في المسار الحالي. |
| RTL | ناجح | تمت معاينة الواجهة على سطح المكتب، والخادم يعمل دون أخطاء TypeScript. |

## ملاحظة تشغيلية

إذا لم يظهر الفرع فورًا، يتأكد المستخدم من اختيار الدور الإداري أو مدير المنطقة ومن إدخال **معرف المنطقة الرقمي** الصحيح، ثم يعيد فتح قسم الفروع. لا يعتمد النظام على صفوف الإجراءات التشغيلية لعرض دليل الفروع؛ بل يستخدم سجلات الفروع الفعلية من `branches.list`.
