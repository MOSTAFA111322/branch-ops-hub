# ملاحظات التدقيق النهائي — 2026-08-24

- ظهر في المعاينة بعد تعديل Vite أن الصفحة بيضاء لأن إعداد `vite.config.ts` صار دالة، بينما `server/_core/vite.ts` يمرره إلى `createViteServer` عبر spread ككائن؛ لذلك لم يُفعّل root الصحيح في بعض عمليات إعادة التشغيل.
- تم تسجيل الخلل في `todo.md` قبل إصلاحه.
- تم إصلاح الإعداد بإعادته إلى كائن `defineConfig({...})`، مع حصر `jsxLocPlugin` في التطوير عبر `process.env.NODE_ENV`، دون تغيير منطق التطبيق.
- بعد الإصلاح أعاد `/src/main.tsx` حالة HTTP 200 ومحتوى Vite محولًا، وأصبح الخادم يعمل على المنفذ 3000.
- رسائل `Auth Missing session cookie` المتبقية في السجلات متوقعة عند فتح المعاينة دون جلسة دخول، وليست تجاوزًا للصلاحيات.
- رسائل Google Maps السابقة كانت سبب إضافة fallback عربي داخل `MapView`، ولا ينبغي أن تعطل بقية لوحة المتابعة.
- يلزم إكمال الاختبار البصري بعد استقرار الخادم، ثم تشغيل TypeScript وVitest والبناء، وتحديث وثيقة الجاهزية وحذف هذا الملف أو إبقاؤه ضمن سجل التدقيق النهائي حسب قرار التسليم.
