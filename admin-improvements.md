# التحسينات الإدارية

أضيفت إلى مركز متابعة وتحسين الفروع واجهة عربية لسجل التدقيق، وهي متاحة للإدارة ومدير المنطقة فقط. تعرض الواجهة وقت العملية، المستخدم ودوره، الفرع، نوع الوحدة، العملية، وتفاصيل التغيير، مع البحث والتصفية وإعادة التحميل. يعتمد العرض على سجلات النظام الفعلية ولا ينشئ بيانات تجريبية.

أضيفت كذلك بطاقة اتجاه الأداء المالي إلى شاشة البيانات المالية، وتعرض آخر ست لقطات شهرية متاحة لكل فترة مختارة، مع مقارنة مرئية بين الإيرادات وصافي الربح وحالات واضحة عند غياب البيانات. وتبقى البيانات مستقلة عن النظام المالي كما هو مقصود في تصميم النظام.

كان تحرير المهام موجودًا في مركز المهام قبل هذه الجولة، ويشمل العنوان والأولوية وتاريخ الاستحقاق والحالة. كما أن تعطيل قوالب قوائم الفحص موجود عبر حقل `isActive` ومسار الصلاحيات الإداري، وهو الأسلوب المعتمد للأرشفة غير التدميرية.

يستهدف التقرير الشهري المجدول أدوار `admin` و`area_manager` و`quality`، ويستخدم سجل التدقيق لتوثيق علامة الفترة والمستلمين ومعرف المهمة. وقد تم التحقق من هذا السلوك عبر الاختبارات الحالية، دون تشغيل إرسال اختباري أو إدخال بيانات تشغيلية مصطنعة.

## التحقق

تم اجتياز فحص TypeScript وجميع اختبارات Vitest وعددها 46 اختبارًا، مع تحقق بصري متجاوب لسطح المكتب والجوال.


## حزمة التحسينات التنفيذية الجديدة

أضيفت إلى النظرة العامة لوحة أداء تنفيذية تعتمد على بيانات `ops.overview` الحية، وتعرض أفضل ثلاثة فروع حسب درجة الصحة مع عدد الإجراءات المفتوحة، وتدعم التبديل بين الفترة اليومية والأسبوعية والشهرية، إضافة إلى رابط التقرير الموحد. تبقى الفلاتر التفصيلية للمنطقة والفرع متاحة في صفحة التقارير الموحدة.

تم تمييز المهام المتأخرة بصريًا في بطاقة مركز المهام الشخصية، مع عرض رسالة «متأخرة» بدل الاكتفاء بتاريخ الاستحقاق. كما أضيفت في سجل التدقيق بطاقة مراجعة التقرير الشهري المجدول، وتعرض آخر فترة مسجلة وحالة التسليم وعدد المستلمين وتاريخ آخر تشغيل. تعتمد البطاقة على سجلات `scheduled_report` الفعلية؛ لذلك يظهر عدم وجود تنفيذ عند غياب السجل بدل عرض حالة مصطنعة.

أصلح التحقق النهائي تحذير CSS الناتج عن استيراد خط Google بعد توسيع Tailwind، ونُقل تحميل الخط إلى `client/index.html`. نتائج الجودة الأخيرة: فحص TypeScript ناجح و46 اختبار Vitest ناجحًا، مع تحقق بصري لسطح المكتب والجوال.

## المقترحات المعتمدة الأخيرة

تمت مراجعة قائمة الفروع التشغيلية الحالية وعددها 15 سجلًا من قاعدة البيانات المنشورة، مع التحقق من أن رمز الفرع واسم الفرع والمدينة والتصنيف حقول منفصلة. لم يقدم المستخدم ملفًا رسميًا جديدًا للمطابقة، لذلك لم يتم اختلاق أو استبدال أي مدينة؛ تم اعتماد السجلات التشغيلية الحالية كما هي.

أضيفت تنبيهات داخلية للمهام التي تجاوزت تاريخ الاستحقاق، مع ربط التنبيه بالفرع أو وسم المهمة العامة وبيان الحاجة إلى التصعيد. أضيف زر «طباعة التقرير» في وحدة التقارير، مع قواعد طباعة RTL تخفي عناصر التحكم وتُبقي نتائج تقرير الالتزام واضحة للطباعة الورقية أو الحفظ كـ PDF من المتصفح.

## حزمة التطوير الشاملة — 2026-08-20

أضيفت مطابقة آمنة لملف Excel تعرض فروقات رمز الفرع والاسم والمدينة قبل اعتماد التحديث، ولا تنشئ أو تحذف فروعًا تلقائيًا. أضيف مركز تاريخ للتنبيهات يعرض المصدر وحالة القراءة محليًا، كما تم تحسين مخرجات التقرير الشهري للطباعة بترويسة وتذييل عربيين وقواعد RTL. تمت مراجعة الفلاتر ومسارات التصعيد الحالية، وأضيفت اختبارات انحدار جديدة. آخر تحقق: TypeScript ناجح و50 اختبار Vitest ناجحًا، مع مراجعة بصرية لسطح المكتب والجوال.

## إضافات الاعتماد والإشعارات

أضيفت جداول وإجراءات الإشعارات الفعلية مع ربطها بالمستخدم المستهدف، ودعم القراءة الفردية والجماعية وتسجيل المصدر والحالة. كما أضيفت شاشة اعتماد التقرير الشهري بتوقيع نصي وحالة وملاحظات وتاريخ اعتماد مرتبط بالمستخدم وسجل التدقيق.

مسار مطابقة Excel جاهز لاستقبال الملف الرسمي، ويعرض الفروقات قبل الحفظ ولا ينفذ أي تعديل تلقائيًا دون اعتماد المستخدم. تم اختبار الواجهة على RTL لسطح المكتب والجوال.
